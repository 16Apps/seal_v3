<%- contentFor('body') %>
  <div ng-controller="itemCtrl" class="app-container">

    <!-- App hero header starts -->
    <div class="app-hero-header d-flex align-items-center">

      <!-- Breadcrumb start -->
      <ol class="breadcrumb d-none d-lg-flex">
        <li class="breadcrumb-item">
          <i class="bi bi-house lh-1"></i>
          <a href="index.html" class="text-decoration-none">Gestão de Itens</a>
        </li>
        <li class="breadcrumb-item" aria-current="page">SKUs</li>
        <li class="breadcrumb-item text-info" aria-current="page">Total: {{ _listItens.length }}</li>
      </ol>
      <!-- Breadcrumb end -->

      <!-- Filter start -->
      <div class="ms-auto d-flex flex-row gap-1 ">
        <div class="search-container d-lg-flex d-none">
          <input ng-model="_pesquisa" ng-change="onPesquisa()" type="text" class="form-control" id="searchData" placeholder="Pesquisar">
          <i class="bi bi-search"></i>
        </div>
        <button ng-click="onItem('add', undefined)" type="button"
          class="btn btn-warning btn-sm ms-3 text-dark">Novo</button>
      </div>
      <!-- Filter end -->

    </div>
    <!-- App Hero header ends -->

    <!-- App body starts -->
    <div class="app-body">

      <div class="modal fade" id="modalItem" tabindex="-1" aria-labelledby="exampleModalFullscreenLabel"
        aria-hidden="true">
        <div class="modal-dialog modal-fullscreen">
          <item funcao="funcaoLoc" edit="editLoc" on-fechar="onItem()"></item>
        </div>
      </div>

      <div class="row gx-3">
        <div class="col-xxl-12">
          <div class="card mb-3">
            <div class="card-body">
              <div class="table-responsive">
                <div class="table-outer">
                  <table class="table align-middle table-hover m-0">
                    <thead>
                      <tr>
                        <th>

                        </th>

                        <th class="w-20" ng-click="sortBy('descricao')" class="sortable">Descrição
                          <i class="bi" ng-class="{
              'bi-caret-up-fill': sortField=='descricao' && !sortReverse,
              'bi-caret-down-fill': sortField=='descricao' && sortReverse
        }"></i>
                        </th>

                        <th class="w-10 sortable" ng-click="sortBy('inf_compl1')">
                          Inf. Compl.
                          <i class="bi" ng-class="{
              'bi-caret-up-fill': sortField=='inf_compl1' && !sortReverse,
              'bi-caret-down-fill': sortField=='inf_compl1' && sortReverse
        }"></i>
                        </th>


                        <th class="w-10 sortable" ng-click="sortBy('id_categoria_reg1.descricao')">
                          Categoria
                          <i class="bi" ng-class="{
              'bi-caret-up-fill': sortField=='id_categoria_reg1.descricao' && !sortReverse,
              'bi-caret-down-fill': sortField=='id_categoria_reg1.descricao' && sortReverse
        }"></i>
                        </th>

                        



                        <th class="w-40 sortable" ng-click="sortBy('tag')">
                          Tag
                          <i class="bi" ng-class="{
              'bi-caret-up-fill': sortField=='tag' && !sortReverse,
              'bi-caret-down-fill': sortField=='tag' && sortReverse
        }"></i>
                        </th>

                        <th class="w-30 sortable" ng-click="sortBy('localizacao')">
                          Localização
                          <i class="bi" ng-class="{
              'bi-caret-up-fill': sortField=='localizacao' && !sortReverse,
              'bi-caret-down-fill': sortField=='localizacao' && sortReverse
        }"></i>
                        </th>

                        <th>Ações</th>
                      </tr>
                    </thead>
                    <tbody>
                      <tr ng-repeat="item in _listItens | orderBy:sortField:sortReverse"">
                      <td>
                          <img class=" rounded-circle img-3x me-2" src="{{ item._foto }}" alt="Wrapmarket" />
                      </td>

                      <td>
                        {{ item.id_categoria.descricao ? item.id_categoria.descricao : 'Item N/A' }}
                        <br>
                        <span ng-show="item.status!='ativo'"
                          class="badge bg-danger bg-opacity-10 text-danger text-uppercase">
                          <i class="bi bi-exclamation-triangle me-1 "></i>{{ item.status }}
                        </span>
                        <span ng-show="item.status=='ativo'"
                          class="badge bg-success bg-opacity-10 text-success text-uppercase">
                          <i class="bi bi-check me-1 "></i>{{ item.status }}
                        </span>
                      </td>

                      <td>{{ item.inf_compl1 }}</td>

                      <td>{{ item.id_categoria_reg1.descricao }}</td>

                      <td>{{ item.tag}}</td>

                      <td>
                        {{ item.id_nivel_loc1 ? item.id_nivel_loc1.descricao : 'Em Transporte' }}
                        {{ item.id_nivel_loc2 ? ' - ' + item.id_nivel_loc2.descricao : '' }}

                        <span ng-show="item.id_nivel_loc3" class="text-muted">
                          <br>
                          {{ item.id_nivel_loc3 ? item.id_nivel_loc3.descricao : '' }}
                          {{ item.id_nivel_loc4 ? ' - ' + item.id_nivel_loc4.descricao : '' }}
                        </span>
                      </td>

                      <td>
                        <a class="btn btn-info btn-sm" ng-click="onItem('edit', item)">
                          <i class="bi bi-pencil"></i>
                        </a>
                      </td>

                      </tr>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

    </div>
    <!-- App body ends -->